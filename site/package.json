{
  "name": "reep-site",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prebuild": "npx tsx scripts/build-data.ts",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "pnpm build && wrangler pages deploy dist --project-name reep-site"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "astro": "^6.1.1",
    "fuse.js": "^7.1.0",
    "typescript": "^6.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsx": "^4.19.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}
